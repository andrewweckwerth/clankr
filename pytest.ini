[pytest]
testpaths = tests/backend
pythonpath = services/orchestrator services/classifier services/acousti
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --strict-markers --disable-socket --allow-unix-socket
timeout = 30
markers =
    integration: real PostgreSQL and Redis; enable with --integration
